[phases.setup]
nixPkgs = ["...", "pkg-config", "cairo", "pango", "libpng", "libjpeg", "giflib", "pixman"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node build/server.js"
